<?xml version="1.0" encoding="utf-8"?>

<!-- Inspired by the Udacity Android Basics by Google Nanodegree Program Course
     Material as well as Soojeong Shin. This code is mostly copied and modified for the
     6th and final project.
     ~ Marvin Tittleton
-->

<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Change Action Bar title text color  -->
        <item name="titleTextColor">@color/color_app_bar_text</item>
        <item name="subtitleTextColor">@color/color_app_bar_text</item>

        <!-- Change Action Bar overflow icon (Options Menu button) color -->
        <item name="actionOverflowButtonStyle">@style/MyOverflowButtonStyle</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Theme of the settings -->
    <style name="SettingsTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Change Action Bar overflow icon (Options Menu button) color -->
    <style name="MyOverflowButtonStyle" parent="Widget.AppCompat.ActionButton.Overflow">
        <item name="android:tint">@color/color_app_bar_text</item>
    </style>

    <!-- App bar style -->
    <style name="MNAAppBarStyle" parent="Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <!-- Remove the shadow below the app bar -->
        <item name="elevation">0dp</item>
    </style>

    <!-- Style for a tab that displays a category name -->
    <style name="CategoryTab" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@android:color/white</item>
        <item name="tabSelectedTextColor">@color/colorAccent</item>
        <item name="tabTextAppearance">@style/CategoryTabTextAppearance</item>
    </style>

    <!-- Text appearance style for a category tab -->
    <style name="CategoryTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textAppearance">?android:textAppearanceSmall</item>
    </style>


    <!-- Style for Title -->
    <style name="TitleTextViewStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingBottom">4dp</item>
        <item name="android:paddingTop">6dp</item>
        <item name="android:textAlignment">center</item>
        <item name="android:textAppearance">?android:textAppearanceLarge</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Style for Body -->
    <style name="BodyViewStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginBottom">@dimen/layout_margin_8</item>
        <item name="android:layout_marginLeft">@dimen/layout_margin</item>
        <item name="android:layout_marginRight">@dimen/layout_margin</item>
        <item name="android:ellipsize">end</item>
        <item name="android:maxLines">6</item>
        <item name="android:textColor">@color/color_body_text</item>
    </style>

    <!-- Layout for Section, Author, and Date -->
    <style name="SectionAuthorDateStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginLeft">@dimen/layout_margin</item>
        <item name="android:layout_marginRight">@dimen/layout_margin</item>
    </style>

    <!-- Style for Section -->
    <style name="SectionViewStyle" parent="@style/SectionAuthorDateStyle">
        <item name="android:layout_alignParentRight">true</item>
        <item name="android:ellipsize">end</item>
        <item name="android:maxLines">1</item>
        <item name="android:textColor">@color/color_section</item>
    </style>

    <!-- Style for Author -->
    <style name="AuthorViewStyle" parent="@style/SectionAuthorDateStyle">
        <item name="android:layout_alignParentRight">true</item>
        <item name="android:textColor">@color/color_author</item>
        <item name="android:textStyle">italic</item>
    </style>

    <!-- Style for Date -->
    <style name="DateViewStyle" parent="@style/SectionAuthorDateStyle">
        <item name="android:layout_alignParentRight">true</item>
        <item name="android:textColor">@color/color_date_text</item>
    </style>

</resources>
